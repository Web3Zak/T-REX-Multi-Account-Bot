# T-REX Multi Account Bot

Автоматизация входа на сайт [T-REX](https://www.trex.xyz) через Twitter/Google и выполнения Daily Check-in для нескольких аккаунтов. Скрипт поддерживает прокси и выбор конкретного аккаунта или всех сразу.

---

## 📋 Возможности

- Поддержка Twitter/Google логина
- Поддержка существующих и новых профилей Chrome
- Поддержка прокси, cookies
- Check-in + Assets Check-in
- Логи в файл

---

## ⚙️ Требования

- Python 3.10+
- Библиотека Playwright

---

# 📝 Описание параметров файла `accounts.json`

### **name**
Название аккаунта (любое).  
Используется в логах и имени профиля Chrome.

### **login_method**
Способ входа на сайт:

| Значение | Описание |
|----------|----------|
| `"google"` | Вход через Google |
| `"twitter"` | Вход через Twitter |

### **remote_debugging_port**
Уникальный порт для управления Chrome.  
Каждый аккаунт должен иметь свой порт:

```
9222
9223
9224
```

### **proxy**
Прокси-сервер для аккаунта.

Примеры:
- Без прокси: `""`
- HTTP-прокси: `"http://127.0.0.1:8080"`
- socks5 c паролем: `"socks5://USERNAME:PASSWORD@HOST:PORT"`

### **cookies**
Используются **только для Twitter**.

- Google игнорирует cookies.
- Twitter требует файл cookies.

### **google_email** и **google_password**
Работают только при `login_method = "google"`.

Два режима:

#### 🔹 Автоматический Google Login  
Если указать email+password — бот сам выполнит вход.

#### 🔹 Ручной Google Login  
Если оставить пустыми — пользователь вводит данные вручную.

---

## 💻 Установка

1. Установите Python 3.10+  
   [Скачать Python](https://www.python.org/downloads/)  
   **Важно:** отметить галочку *Add Python to PATH* при установке.

2. Настройте файл `accounts.json`

3. Запустите файл start.bat
